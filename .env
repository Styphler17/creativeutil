# Maintenance Mode Configuration
# Set to 'true' to activate full-site maintenance mode (renders Maintenance page for all routes).
# Default: false (normal site operation).
# To activate: Change to true, rebuild/deploy.
VITE_MAINTENANCE_MODE=false